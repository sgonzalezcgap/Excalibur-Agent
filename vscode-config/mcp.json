{
  "//NOTE": "This is a TEMPLATE. setup.bat generates the actual mcp.json with absolute paths.",
  "//NOTE2": "Do NOT copy this file manually — run setup.bat instead.",
  "servers": {
    "excalibur-agent": {
      "type": "stdio",
      "command": "${env:LOCALAPPDATA}\\Programs\\Python\\Python312\\python.exe",
      "args": [
        "${workspaceFolder}/Upgraded/Excalibur-Agent/mcp_server.py"
      ],
      "env": {
        "PYTHONIOENCODING": "utf-8"
      }
    }
  }
}
